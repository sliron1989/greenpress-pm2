{
  "name": "greenpress",
  "version": "1.0.0",
  "description": "fully blog system with SSR using node.js",
  "main": "index.js",
  "engines": {
    "npm": ">=5",
    "node": ">=10.5"
  },
  "scripts": {
    "install:auth": "(cd auth && npm install)",
    "install:content": "(cd content && npm install)",
    "install:front": "(cd blog-front && npm install && npm run build)",
    "sync-modules": "node helpers/install-sub-modules.js",
    "postinstall": "npm run sync-modules && npm run install:auth && npm run install:content && npm run install:front",
    "start": "pm2-runtime start ecosystem.config.js --env production",
    "dev": "pm2-runtime start pm2-dev.config.js --env development",
    "init": "node helpers/init-data.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pm2": "^3.5.1"
  }
}
